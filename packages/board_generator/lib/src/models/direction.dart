/// {@template direction}
/// The direction of a word on the board.
/// {@endtemplate}
enum Direction {
  /// The word is across the board, read from left to right.
  across,

  /// The word is down the board, read from top to bottom.
  down,
}
