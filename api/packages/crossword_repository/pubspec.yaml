name: crossword_repository
description: Repository to access the crossword data
version: 0.1.0+1
publish_to: none

environment:
  sdk: "^3.3.0"

dependencies:
  clock: ^1.1.1
  db_client:
    path: ../db_client
  game_domain:
    path: ../game_domain

dev_dependencies:
  mocktail: ^1.0.3
  test: ^1.25.2
  very_good_analysis: ^5.1.0
