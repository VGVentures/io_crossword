name: leaderboard_repository
description: Leaderboard repository
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  collection: ^1.18.0
  db_client:
    path: ../db_client
  game_domain:
    path: ../game_domain
  meta: ^1.14.0

dev_dependencies:
  mocktail: ^1.0.0
  test: ^1.19.2
  very_good_analysis: ^6.0.0
